# Minimal deps for the STAGE-1 OCR workflow only (CPU / Mac-friendly).
# This is intentionally NOT the full project stack (pyproject.toml pulls
# torch/torchvision/sam2/hydra for the GPU tracking pipeline — not needed
# to read the instrument strip).
#
# Install into a fresh venv:
#   python3 -m venv .venv-ocr && source .venv-ocr/bin/activate
#   pip install -r requirements-ocr.txt
#
# PaddleOCR 3.x does NOT auto-install the inference engine — paddlepaddle must
# be listed explicitly (this was missing and caused a "paddle_static engine
# unavailable" error). The OCR script auto-detects the PaddleOCR 2.x vs 3.x API.
# If paddlepaddle has no wheel for your platform, see https://www.paddlepaddle.org.cn

paddleocr>=2.7
paddlepaddle>=2.5
rapidfuzz>=3.0
opencv-python>=4.7
numpy>=1.24
matplotlib>=3.7
