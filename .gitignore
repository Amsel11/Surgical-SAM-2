.vscode/
.idea/
.DS_Store
__pycache__/
*-checkpoint.ipynb
.venv
*.egg*
build/*
_C.*
outputs/*
checkpoints/*.pt
checkpoints/*.pth
demo/backend/checkpoints/*.pt
datasets/*
model_weights/*
/sav_dataset/example/
results/
test_data/
# Repo-root prompts/ holds clicker output JSON (not committed). The
# pipeline/prompts/ Python package IS committed — leading slash anchors
# the pattern to the repo root so it doesn't catch the package dir.
/prompts/
archive/
.claude/

# Manifest DB is live state on bigpurple. Snapshots (manifest_snapshot.*) ARE committed.
manifest.db
manifest.db-wal
manifest.db-shm

# Local working state on olab-1 (the SQLite WAL files + working dirs)
local_manifest.db
local_manifest.db-wal
local_manifest.db-shm
local_frames/
local_results/

# Per-machine paths and credentials. Copy .env.example to .env and edit.
.env
.env.local
