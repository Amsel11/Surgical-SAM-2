# @package stage2_inference
# SAM 3 (not yet released as of repo creation). Placeholder so ablation
# configs can reference it; runner will raise NotImplementedError until the
# sam3 model handler is added to pipeline/models/.
model: sam3
checkpoint: ${oc.env:BP_REPO,.}/checkpoints/sam3.pth
config: configs/sam3/sam3_base.yaml
device: cuda:0
bidirectional: true
outputs:
  masks: true
  overlay_video: true
  overlay_jpgs: false
  preview_small: true
