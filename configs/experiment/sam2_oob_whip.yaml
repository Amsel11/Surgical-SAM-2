# @package _global_
# Ablation level 1: stock SAM 2.1 hiera-s (no surgical fine-tuning), manual
# boxes, whip cohort. Establishes the floor.
#
# Run: python -m pipeline run +experiment=sam2_oob_whip

name: sam2_oob_whip_v1
description: Stock SAM 2.1 hiera-s, manual_box prompts, whip cohort, OOB

stage2_inference:
  model: sam2
  checkpoint: ${oc.env:BP_REPO,.}/checkpoints/sam2.1_hiera_small.pt
